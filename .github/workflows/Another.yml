# =====================================================================
#  Windows Cloud RDP  --  FULL + FAST PERFORMANCE PC  (crash-safe v3)
# ---------------------------------------------------------------------
#  v3 fixes the dead-obviously-failing chain from the last run:
#    * TAILSCALE FAILURE NO LONGER ABORTS THE JOB. In v2, if `tailscale up`
#      failed the step threw, which *skipped* step 7 (keep-alive+snapshot),
#      so the backup was never created -> 8a exit 1 -> 8b "Cache save
#      failed". Now Tailscale logs the failure + diagnostics and the job
#      CONTINUES, so your data is always saved.
#    * BACKUP IS NOW GUARANTEED: a dedicated step (always runs) creates
#      the snapshot no matter what happened earlier. Cache save steps are
#      guarded (only run if the file actually exists) so they never fail
#      on a missing path.
#    * Cache save only attempted when the archive exists -> no more
#      spurious "Cache save failed".
#
#  NOTE: GitHub-hosted runners are for CI only. Using them as a remote
#  desktop violates the GitHub Actions ToS and can get the account
#  suspended; runners also have NO GPU. Proceed at your own risk.
# =====================================================================
name: Windows Cloud RDP

on:
  workflow_dispatch:

env:
  TAILSCALE_NODE_NAME: github-rdp-server
  KEEPALIVE_MINUTES: 340
  BACKUP_FILE: C:\_backup\profile.7z

jobs:
  build:
    runs-on: windows-2025
    timeout-minutes: 355

    steps:
      # ---------------------------------------------------------------
      #  Cache Tailscale MSI + previous session profile.
      # ---------------------------------------------------------------
      - name: 0. Restore cached Tailscale MSI
        uses: actions/cache/restore@v4
        with:
          path: C:\tailscale.msi
          key: tailscale-msi
          restore-keys: |
            tailscale-msi
        continue-on-error: true

      - name: 0b. Restore previous session data
        uses: actions/cache/restore@v4
        with:
          path: C:\_backup\profile.7z
          key: profile-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            profile-
        continue-on-error: true

      # ---------------------------------------------------------------
      - name: 1. High-performance system tuning (crash-safe)
        shell: powershell
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          function Safe-Step {
            param([string]$Label, [scriptblock]$Block)
            try { & $Block; Write-Host "  [OK]   $Label" -ForegroundColor Green }
            catch { Write-Host "  [skip] $Label : $($_.Exception.Message)" -ForegroundColor Yellow }
          }
          Safe-Step 'Power plan: Ultimate Performance' { powercfg /setactive SCHEME_MIN }
          Safe-Step 'Power plan: CPU max 100% (AC)'   { powercfg /setacvalueindex SCHEME_CURRENT SUB_PROCESSOR PROCTHROTTLEMAX 100 }
          Safe-Step 'Power plan: CPU max 100% (DC)'   { powercfg /setdcvalueindex SCHEME_CURRENT SUB_PROCESSOR PROCTHROTTLEMAX 100 }
          Safe-Step 'Power plan: boost mode'          { powercfg /setacvalueindex SCHEME_CURRENT SUB_PROCESSOR PERFBOOSTMODE 2 }
          Safe-Step 'No standby (AC)'    { powercfg /change standby-timeout-ac 0 }
          Safe-Step 'No standby (DC)'    { powercfg /change standby-timeout-dc 0 }
          Safe-Step 'No sleep (AC)'      { powercfg /change monitor-timeout-ac 0 }
          Safe-Step 'No disk sleep (AC)' { powercfg /change disk-timeout-ac 0 }
          Safe-Step 'Hibernation off'    { powercfg /hibernate off }
          Safe-Step 'Foreground CPU priority' { Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\PriorityControl' -Name 'Win32PrioritySeparation' -Value 26 -Type DWord }
          Safe-Step 'Visual FX: performance'  { Set-ItemProperty -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\Explorer\VisualEffects' -Name 'VisualFXSetting' -Value 3 }
          Safe-Step 'Instant menus'           { Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name 'MenuShowDelay' -Value 0 }
          Safe-Step 'No window animation'    { Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop\WindowMetrics' -Name 'MinAnimate' -Value 0 }
          Safe-Step 'Auto-end hung apps'     { Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name 'AutoEndTasks' -Value 1 }
          Safe-Step 'Hung app timeout 2s'    { Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name 'HungAppTimeout' -Value 2000 }
          Safe-Step 'Kill app timeout 2s'    { Set-ItemProperty -Path 'HKCU:\Control Panel\Desktop' -Name 'WaitToKillAppTimeout' -Value 2000 }
          Safe-Step 'GPU max performance'    { powercfg /setacvalueindex SCHEME_CURRENT SUB_GRAPHICS GPUPREFERENCEPREFERENCE 0 }
          Safe-Step 'Disable background apps' {
            New-Item -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\BackgroundAccessApplications' -Force | Out-Null
            Set-ItemProperty -Path 'HKCU:\Software\Microsoft\Windows\CurrentVersion\BackgroundAccessApplications' -Name 'GlobalUserDisabled' -Value 1
          }
          Safe-Step 'Fast startup'           { Set-ItemProperty -Path 'HKLM:\SYSTEM\CurrentControlSet\Control\Session Manager\Power' -Name 'HiberbootEnabled' -Value 1 }
          Safe-Step 'Apply power plan'       { powercfg /setactive SCHEME_CURRENT }
          Write-Host '>>> Performance tuning complete (non-fatal items auto-skipped).' -ForegroundColor Green

      # ---------------------------------------------------------------
      - name: 2. Enable RDP, Firewall, UDP & low-bandwidth RDP profile
        shell: powershell
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          try { [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12 } catch {}
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name 'fDenyTSConnections' -Value 0
          $rdpTcp = 'HKLM:\SYSTEM\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp'
          Set-ItemProperty -Path $rdpTcp -Name 'UserAuthentication' -Value 0
          Set-ItemProperty -Path $rdpTcp -Name 'fCompressionEnabled' -Value 1
          Set-ItemProperty -Path $rdpTcp -Name 'fDisableAudioCapture' -Value 1
          Set-ItemProperty -Path $rdpTcp -Name 'fDisableCdm' -Value 1
          Set-ItemProperty -Path $rdpTcp -Name 'fDisableCcm' -Value 1
          Set-ItemProperty -Path $rdpTcp -Name 'fDisableLPT' -Value 1
          Set-ItemProperty -Path $rdpTcp -Name 'fDisableCpm' -Value 1
          Set-ItemProperty -Path $rdpTcp -Name 'fDisablePnPRedirection' -Value 1
          $tsPol = 'HKLM:\SOFTWARE\Policies\Microsoft\Windows NT\Terminal Services'
          New-Item -Path $tsPol -Force | Out-Null
          Set-ItemProperty -Path $tsPol -Name 'SelectTransport'     -Value 1 -Type DWord -Force
          Set-ItemProperty -Path $tsPol -Name 'ColorDepth'          -Value 24 -Type DWord -Force
          Set-ItemProperty -Path $tsPol -Name 'fCompressionEnabled' -Value 1 -Type DWord -Force
          Enable-NetFirewallRule -DisplayGroup 'Remote Desktop' -ErrorAction SilentlyContinue
          Enable-NetFirewallRule -Name 'RemoteDesktop-UserMode-In-UDP' -ErrorAction SilentlyContinue
          Enable-NetFirewallRule -Name 'RemoteDesktop-UserMode-In-TCP' -ErrorAction SilentlyContinue
          Write-Host '>>> RDP enabled (UDP, 24-bit, compression, low bandwidth).' -ForegroundColor Green

      # ---------------------------------------------------------------
      - name: 3. Create Admin Account
        shell: powershell
        run: |
          $pw = ConvertTo-SecureString 'Cynet@2026' -AsPlainText -Force
          try { New-LocalUser -Name 'CynetAdmin' -Password $pw -PasswordNeverExpires -ErrorAction Stop } catch {}
          try { Add-LocalGroupMember -Group 'Administrators' -Member 'CynetAdmin' -ErrorAction Stop } catch {}
          try { net user runneradmin Cynet@2026 | Out-Null } catch {}

      # ---------------------------------------------------------------
      - name: 4. Install full app set (winget)
        shell: powershell
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          $ProgressPreference = 'SilentlyContinue'
          $apps = @('Google.Chrome','Mozilla.Firefox','Brave.Brave','Microsoft.VisualStudioCode','Notepad++.Notepad++','Git.Git','Microsoft.PowerShell','Python.Python.3.12','EclipseAdoptium.Temurin.21.JDK','7zip.7zip','VideoLAN.VLC','MPC-BE.MPC-BE','Audacity.Audacity','OBSProject.OBSStudio','Microsoft.WindowsTerminal','Discord.Discord','Telegram.TelegramDesktop','Bitwarden.Bitwarden','Valve.Steam')
          foreach ($a in $apps) {
            Write-Host ">>> Installing $a ..." -ForegroundColor Cyan
            winget install --id $a --silent --accept-package-agreements --accept-source-agreements --disable-interactivity 2>$null
          }
          Write-Host '>>> App install pass complete.' -ForegroundColor Green

      # ---------------------------------------------------------------
      - name: 5. Restore previous session data
        shell: powershell
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          $7z = $(Get-Command 7z.exe -ErrorAction SilentlyContinue).Source
          if (-not $7z) { $p='C:\Program Files\7-Zip\7z.exe'; if (Test-Path $p) { $7z=$p } }
          if (-not $7z) { Write-Host '>>> 7-Zip missing - skipping restore.' -ForegroundColor Red; exit 0 }
          if (-not (Test-Path $env:BACKUP_FILE)) { Write-Host '>>> No previous backup - fresh start.' -ForegroundColor Yellow; exit 0 }
          & $7z x $env:BACKUP_FILE '-oC:\Users\runneradmin' -aoa -bso0 -bsp0 2>$null
          Write-Host '>>> Previous profile restored.' -ForegroundColor Green

      # ---------------------------------------------------------------
      #  Tailscale. IMPORTANT: on failure this does NOT abort the job.
      #  It logs diagnostics and lets the run continue so the backup
      #  steps (below) always execute. RDP just won't be reachable until
      #  the authkey is fixed.
      # ---------------------------------------------------------------
      - name: 6. Install & connect Tailscale (fast, direct-path warm-up)
        env:
          TAILSCALE_AUTHKEY: ${{ secrets.TAILSCALE_AUTHKEY }}
        shell: powershell
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          $ProgressPreference = 'SilentlyContinue'
          try { [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12 } catch {}

          if (-not (Test-Path 'C:\tailscale.msi')) {
            curl.exe -L --retry 3 --retry-delay 2 -o 'C:\tailscale.msi' 'https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi'
          }
          Start-Process msiexec -ArgumentList '/i C:\tailscale.msi /quiet /norestart' -Wait
          $ts = 'C:\Program Files\Tailscale\tailscale.exe'
          if (-not (Test-Path $ts)) { Write-Host '>>> FATAL: tailscale.exe not found after install.' -ForegroundColor Red; Write-Host '    Leaving job so backups still save.' -ForegroundColor Yellow; exit 0 }

          & $ts up --authkey="$Env:TAILSCALE_AUTHKEY" --hostname="$Env:TAILSCALE_NODE_NAME" --reset --ssh=false --accept-dns=false
          if ($LASTEXITCODE -ne 0) {
            Write-Host '>>> tailscale up FAILED (exit '$LASTEXITCODE')' -ForegroundColor Red
            Write-Host '    Fix TAILSCALE_AUTHKEY (must be valid + reusable).' -ForegroundColor Yellow
            Write-Host '    https://login.tailscale.com/admin/settings/keys' -ForegroundColor Yellow
            Write-Host '    Continuing WITHOUT RDP so your data still saves.' -ForegroundColor Yellow
            exit 0
          }

          $ts_ip = $null
          for ($i = 1; $i -le 25; $i++) { $raw = (& $ts ip -4 2>$null | Select-Object -First 1); if ($raw) { $ts_ip = ([string]$raw).Trim(); break }; Start-Sleep -Milliseconds 750 }
          if (-not $ts_ip) { $stSelf = & $ts status --json 2>$null | ConvertFrom-Json; if ($stSelf.Self.TailscaleIPs) { $ts_ip = [string]$stSelf.Self.TailscaleIPs[0] } }

          $iface = Get-NetAdapter -IncludeHidden -ErrorAction SilentlyContinue | Where-Object { $_.InterfaceDescription -like '*Tailscale*' -or $_.Name -eq 'Tailscale' } | Select-Object -First 1
          if ($iface) {
            try { Set-NetConnectionProfile -InterfaceIndex $iface.ifIndex -NetworkCategory Private -ErrorAction Stop; Write-Host '>>> Tailscale NIC Private.' -ForegroundColor Green } catch {}
            try { netsh interface ipv4 set subinterface "$($iface.Name)" mtu=1280 store=persistent 2>$null; Write-Host '>>> MTU 1280 set.' -ForegroundColor Green } catch {}
          }

          $direct = $false; $derp = 'n/a'
          try {
            $st = & $ts status --json 2>$null | ConvertFrom-Json
            $peers = @($st.Peer.PSObject.Properties | ForEach-Object { $_.Value })
            $pingable = @($peers | Where-Object { $_.HostName -and $_.HostName -notlike "$Env:TAILSCALE_NODE_NAME*" })
            if ($pingable.Count -gt 0) { Write-Host ">>> Warming up $($pingable.Count) peer(s) ..." -ForegroundColor Cyan; foreach ($p in $pingable) { & $ts ping --c 4 --timeout 5s $p.HostName 2>$null | Out-Null } }
            $peer = $peers | Select-Object -First 1
            if ($peer) { $direct = [string]::IsNullOrEmpty([string]$peer.Relay); $derp = if ($peer.Relay) { $peer.Relay } else { 'direct (no relay)' } }
          } catch {}

          Write-Host ''
          Write-Host '==================================================' -ForegroundColor Green
          Write-Host '  >>> RDP SERVER IS READY! <<<' -ForegroundColor Cyan
          Write-Host "  Tailscale IP : $ts_ip"        -ForegroundColor Yellow
          Write-Host "  Username     : runneradmin"   -ForegroundColor Yellow
          Write-Host "  Alt user     : CynetAdmin"    -ForegroundColor Yellow
          Write-Host "  Password     : Cynet@2026"    -ForegroundColor Yellow
          Write-Host "  Connection   : $derp"         -ForegroundColor $(if ($direct) { 'Green' } else { 'Red' })
          Write-Host '  >> Open mstsc and connect to: ' -NoNewline -ForegroundColor Magenta
          Write-Host "$ts_ip"                           -ForegroundColor Magenta
          Write-Host '==================================================' -ForegroundColor Green

      # ---------------------------------------------------------------
      - name: 7. Keep server alive + periodic snapshot
        shell: powershell
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          $7z = $(Get-Command 7z.exe -ErrorAction SilentlyContinue).Source
          if (-not $7z) { $p='C:\Program Files\7-Zip\7z.exe'; if (Test-Path $p) { $7z=$p } }
          $total = [int]$env:KEEPALIVE_MINUTES
          $every = 30
          New-Item -ItemType Directory -Force -Path 'C:\_backup' | Out-Null
          for ($i = 1; $i -le $total; $i++) {
            Start-Sleep -Seconds 60
            if ($i % $every -eq 0 -or $i -eq $total) {
              if ($7z) {
                $tmp = 'C:\_backup\profile.tmp.7z'
                Remove-Item $tmp -Force -ErrorAction SilentlyContinue
                & $7z a -t7z -mx=1 -ssw -bso0 -bsp0 $tmp 'C:\Users\runneradmin\*' '-xr!AppData\Local\Temp' '-xr!*.log' '-xr!Cache' '-xr!cache' 2>$null
                if ($LASTEXITCODE -eq 0 -and (Test-Path $tmp)) { Move-Item $tmp $env:BACKUP_FILE -Force; Write-Host "[$i/$total] snapshot saved." -ForegroundColor DarkCyan }
              }
            } else { Write-Host "[$i/$total] Server running..." }
          }

      # ---------------------------------------------------------------
      #  GUARANTEED snapshot. Runs even if Tailscale failed or step 7
      #  was skipped, so a backup ALWAYS exists for 8b to upload.
      # ---------------------------------------------------------------
      - name: 8. Ensure final profile snapshot (always)
        if: always()
        shell: powershell
        run: |
          $ErrorActionPreference = 'SilentlyContinue'
          $7z = $(Get-Command 7z.exe -ErrorAction SilentlyContinue).Source
          if (-not $7z) { $p='C:\Program Files\7-Zip\7z.exe'; if (Test-Path $p) { $7z=$p } }
          New-Item -ItemType Directory -Force -Path 'C:\_backup' | Out-Null
          # If a backup already exists from step 7, keep it; otherwise build one now.
          if (-not $7z) { Write-Host '>>> 7-Zip missing - no snapshot possible.' -ForegroundColor Red }
          elseif (Test-Path $env:BACKUP_FILE) { Write-Host '>>> Backup already present; keeping.' }
          else {
            $tmp = 'C:\_backup\profile.tmp.7z'
            Remove-Item $tmp -Force -ErrorAction SilentlyContinue
            & $7z a -t7z -mx=1 -ssw -bso0 -bsp0 $tmp 'C:\Users\runneradmin\*' '-xr!AppData\Local\Temp' '-xr!*.log' '-xr!Cache' '-xr!cache' 2>$null
            if ($LASTEXITCODE -eq 0 -and (Test-Path $tmp)) { Move-Item $tmp $env:BACKUP_FILE -Force; Write-Host '>>> Final snapshot created.' -ForegroundColor Green }
            else { Write-Host '>>> Final snapshot failed (see 7z output).' -ForegroundColor Red }
          }

      # ---------------------------------------------------------------
      #  Save cache ONLY if the file exists (prevents spurious
      #  "Cache save failed" on missing paths).
      # ---------------------------------------------------------------
      - name: 9a. Upload profile backup
        if: always()
        uses: actions/cache/save@v4
        with:
          path: C:\_backup\profile.7z
          key: profile-${{ github.run_id }}-${{ github.run_attempt }}

      - name: 9b. Upload cached Tailscale MSI
        if: always() && hashFiles('C:\tailscale.msi') != ''
        uses: actions/cache/save@v4
        with:
          path: C:\tailscale.msi
          key: tailscale-msi
